# ------------------------------------------------------------------
# myTTS — Python dependencies
#
# Install:
#   pip install -r requirements.txt
#
# CosyVoice (the TTS model framework) is NOT on PyPI.
# Install it separately from source:
#   git clone --recursive https://github.com/FunAudioLLM/CosyVoice.git
#   cd CosyVoice
#   pip install -r requirements.txt
# ------------------------------------------------------------------

# --- Web framework ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.6

# --- Frontend ---
streamlit>=1.30,<2.0
requests>=2.31

# --- Audio processing ---
numpy>=1.24
pydub>=0.25

# --- ML / model inference (install torch with appropriate CUDA/CPU index) ---
torch>=2.1
torchaudio>=2.1
huggingface_hub>=0.20

# --- CosyVoice model dependencies (also installed when you install CosyVoice) ---
# transformers>=4.36
# onnxruntime>=1.15
# onnx>=1.15

# --- Testing ---
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1
pytest-mock>=3.12
httpx>=0.26
